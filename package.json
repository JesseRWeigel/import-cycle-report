{
  "name": "import-cycle-report",
  "version": "0.1.0",
  "description": "Find import cycles that actually break at runtime and emit one self-contained HTML report",
  "type": "module",
  "bin": {
    "import-cycle-report": "bin/cycles.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "typescript": "5.9.3"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify": "bash scripts/verify.sh"
  },
  "devDependencies": {
    "playwright-core": "1.58.2"
  }
}
